import { Text } from 'react-native'
import { FridgeScreenProps } from '../../types/Types'

const Fridge = ({ navigation, route }: FridgeScreenProps) => {
  return <Text>Fridge</Text>
}
export default Fridge
